int main(int argc) {
	
	switch (argc) {
		case 1: break;
		default: break;
	}
LOOP:do {break;continue;}while(1);
	while (1) {break;}

	int n;
	for (int i = 0; i < n; ++i);
	goto LOOP;
	if (n)
		;
	else
		;
	return 0;
}
